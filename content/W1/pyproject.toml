[project]
name = "w1"
version = "0.1.0"
description = "Automation with Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "pyyaml>=6.0.3",
]
